<%
    start_at = @setup.first_week_at.to_date + (@week.number-1) * 7
    end_at = start_at + 6
%>
<div class="nav-info mobile-content">
  <%= start_at.strftime("%d.%-m.")  %> - <%= end_at.strftime("%d.%-m.%Y")  %>
  <% if current_user.has_rooms? %>
      <div class="week-progressbar" data-number-all="<%= @week.rooms_count current_user.id %>" data-number-done="<%= @week.rooms_count_done current_user.id %>">
        <div class="progressbar-caption"></div>
      </div>
  <% else %>
      <div class="week-progressbar" data-number-all="<%= @sorted_los.count %>" data-number-done="<%= @results.select {|r| not (r['solved'].nil? or r['solved'].to_i.zero?) }.count %>">
        <div class="progressbar-caption"></div>
      </div>
  <% end %>

  <div id="week-toggle" class="toggle">
    <div class="toggle-caption">Režim
    </div>
    <div class="toggle-item-list">
      <div class="toggle-item <%= 'active' unless current_user.show_solutions %>" id="week-toggle-testing">
        Testovanie
      </div><div class="toggle-item <%= 'active' if current_user.show_solutions %>" id="week-toggle-learning">
        Učenie
      </div>
    </div>
    <div class="toggle-caption">
      <i class="icon-help toggle-help-btn"></i>
    </div>
    <div class="toggle-help" id="week-toggle-help">
      V režime <strong>Testovanie</strong> sa môžete otestovať riešením otázok. V režime <strong>Učenie</strong> sa Vám okamžite zobrazia otázky aj so správnymi odpovedami.
    </div>
  </div>
</div>
