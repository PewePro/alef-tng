(function() {
    var toggle = $('#week-toggle');
    Toggle.reset(toggle);
    <% if @user.show_solutions %>
        Toggle.select(toggle,'learning');
    <% else %>
        Toggle.select(toggle,'testing');
    <% end %>
})();

